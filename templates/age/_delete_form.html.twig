<form method="post" action="{{ path('app_age_delete', {'id': age.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette tranche d'âge ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ age.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
