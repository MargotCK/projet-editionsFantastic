{% extends 'base.html.twig' %}

{% block h1 %}{{age.tranche}}{% endblock %}

{% block body %}

    <a class="btn btn-dark" href="{{ path('app_age_index') }}">Retour</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ age.id }}</td>
            </tr>
            <tr>
                <th>Tranche</th>
                <td>{{ age.tranche }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-2" href="{{ path('app_age_edit', {'id': age.id}) }}">Modifier</a>

    {{ include('age/_delete_form.html.twig') }}
{% endblock %}
