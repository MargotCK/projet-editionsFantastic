{% extends 'base.html.twig' %}

{% block h1 %}{{ auteur.nomAuteur }}{% endblock %}

{% block body %}

    <a class="btn btn-dark" href="{{ path('app_auteur_index') }}">Retour</a>
    
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ auteur.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ auteur.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ auteur.nomAuteur }}</td>
            </tr>
        </tbody>
    </table>

    

    <a class="btn btn-primary mb-2" href="{{ path('app_auteur_edit', {'id': auteur.id}) }}">Modifier</a>

    {{ include('auteur/_delete_form.html.twig') }}
{% endblock %}
