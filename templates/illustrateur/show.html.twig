{% extends 'base.html.twig' %}

{% block h1 %}{{ illustrateur.nom }}{% endblock %}

{% block body %}
    
        <a class="btn btn-dark" href="{{ path('app_illustrateur_index') }}">Retour</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ illustrateur.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ illustrateur.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ illustrateur.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary mb-2" href="{{ path('app_illustrateur_edit', {'id': illustrateur.id}) }}">Modifier</a>

    {{ include('illustrateur/_delete_form.html.twig') }}
{% endblock %}
