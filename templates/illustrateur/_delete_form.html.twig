<form method="post" action="{{ path('app_illustrateur_delete', {'id': illustrateur.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet illustrateur(trice) ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ illustrateur.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
