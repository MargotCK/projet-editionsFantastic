{% extends 'base.html.twig' %}

{% block h1 %}{{ collection.categorieCollection }}{% endblock %}

{% block body %}
    <a class="btn btn-dark" href="{{ path('app_collection_index') }}">Retour</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ collection.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ collection.categorieCollection }}</td>
            </tr>
        </tbody>
    </table>

    

    <a class="btn btn-primary mb-2" href="{{ path('app_collection_edit', {'id': collection.id}) }}">Modifier</a>

    {{ include('collection/_delete_form.html.twig') }}
{% endblock %}
